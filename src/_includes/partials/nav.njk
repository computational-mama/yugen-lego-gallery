{% set navigation = collections.all | eleventyNavigation %}
<header class="top-nav"  id="header-banner">
    <div class="intro">
        <div>
            <a class="home-button" href="/"><h1>{{ site.author.firstName }} {{ site.author.lastName }}</h1></a>
            <p class="home-about">{{ site.author.about }}</p>
        </div>
    </div>
    <nav class="nav-main">
        <ul class="nav-list">
            {% for link in navigation %}
            <li><a href="{{ link.url | url }}" class="link">{{ link.title }}</a></li>
            {% endfor %}
        </ul>
    </nav>
</header>